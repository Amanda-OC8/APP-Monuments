<h2>Monumentos</h2>

<div class="row">
    {{#each allMonuments}}
    <article class="col-md-4">
        <a href="/monuments/{{_id}}">
            <h4>{{title}}</h4>
        </a>
        <div id="map"></div>
        <p class="text-secondary">{{address.street}}, {{address.locality}}</p>
    </article>
    {{/each}}

</div>
